/**
 * Storefront Section — re-exports shared layout Section.
 * Keeps a single spacing contract across homepage blocks.
 */
export { Section } from "@/shared/components/Section";
export { Container } from "@/shared/components/Container";
